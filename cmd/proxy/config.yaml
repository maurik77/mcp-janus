proxy:
  # base_url: https://suboral-wiley-predesirously.ngrok-free.dev
  base_url: http://localhost:8080
  listen_addr: ":8080"
  log_level: debug
  log_format: json

idp:
  client_id: 379eab96-a9ad-401f-8158-19b130fefbf3
  client_secret: supersecret123
  openid_configuration_url: https://swelfad01azne.b2clogin.com/swelfad01azne.onmicrosoft.com/v2.0/.well-known/openid-configuration?p=B2C_1A_HOME_REALM_DISCOVERY
  jwt_leeway: 60s  # in seconds
  scopes:
    - openid
    # - profile
    - offline_access
    - 379eab96-a9ad-401f-8158-19b130fefbf3
  claims_mapping:
    sub: X_sub
    name: X_full_name
    email: X_email
    upn: X-USER

  # Keycloak: delegate MCP client registration to Keycloak via RFC 7591 DCR
  # registration_mode: delegate           # "local" (default) or "delegate"
  # registration_initial_token: ""        # Keycloak initial access token (or empty for trusted hosts)

  # JWT validation (required for Keycloak tokens)
  # validate_audience: true
  # audience: http://localhost:8081       # upstream resource URI
  # validate_issuer: true                 # uses issuer from OIDC discovery

encryption:
  master_key: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

telemetry:
  enabled: false

upstream:
  name: weather-test
  # resource: http://localhost:8085
  # base_url: http://localhost:8085
  resource: http://localhost:8081
  base_url: http://localhost:8081
  path_prefix: 
